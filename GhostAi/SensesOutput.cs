using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SensesOutput : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void ObjectSeen(GameObject objectSeen)
	{
        // highlight object
        // if ai then add to priorities
	}

    void NoiseHeard(GameObject objectHeard)
	{
        // highlight object
        // if ai then add to priorities
    }

    void EMFSensed(Transform emfArea)
	{
        // highlight emf area
        // if ai then add to priorities
    }
}
